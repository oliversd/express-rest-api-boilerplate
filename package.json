{
  "name": "api-boilerplate",
  "version": "0.0.1",
  "description": "A boilerplate for Node, Express, ES6/7 API",
  "main": "index.js",
  "scripts": {
    "start": "DEBUG='*, -nodemon, -nodemon:*, -babel'  nodemon server/index.js --exec babel-node",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run lint && npm run clean && mkdir dist && babel server -s -d dist && npm run serve",
    "serve": "DEBUG=* node dist/index.js",
    "lint:watch": "esw -f simple-detail -w *.js server",
    "lint": "eslint *.js server"
  },
  "keywords": [
    "api",
    "node",
    "express",
    "es6",
    "es7",
    "api"
  ],
  "author": "Olivers De Abreu <olivers@vensign.com> (http://vensign.com/)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-eslint": "^7.1.1",
    "babel-preset-env": "^1.2.0",
    "babel-preset-stage-2": "^6.22.0",
    "dotenv": "^4.0.0",
    "eslint": "^3.17.0",
    "eslint-config-airbnb-base": "11.1.1",
    "eslint-plugin-import": "2.2.0",
    "eslint-watch": "^3.0.1",
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "debug": "^2.6.1",
    "express": "^4.15.2"
  }
}
